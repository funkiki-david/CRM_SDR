# SDR CRM — Deployment Log (April 16-17, 2026)

## Summary

Two-day sprint to deploy the entire system from local MacBook to Railway cloud.
Both backend and frontend are now live with 117 real contacts imported.

---

## Live URLs

| Service | URL |
|---------|-----|
| Backend API | https://crmsdr-production.up.railway.app |
| Frontend UI | https://zealous-enjoyment-production.up.railway.app |

---

## Day 1 (April 16) — Code Changes + Backend Deploy

### 1. Contacts Page Enhancements (before deployment)
- **Add Contact modal**: Full form with validation (9 fields), email dedup detection (View/Update/Duplicate options), cancel confirmation, auto-select after save
- **New fields added to database**: `city`, `state`, `website`, `notes`, `industry_tags_arr`, `import_source`, `import_batch_id`
- **Website migration**: Wrote script to extract website URLs from notes field → 92 contacts updated
- **Contact detail page**: LinkedIn + Website displayed as clickable links, "Not available" + [+ Add] button for missing data
- **Checkbox multi-select**: Every contact row now has a checkbox, with Select All

### 2. AI System Overhaul
- **Unified to single provider**: All AI features now use Anthropic Claude (Haiku 4.5) only
- **Removed OpenAI dependency**: Embedding-based search replaced with Claude direct search (reads activities and finds relevant ones)
- **Single API key**: Settings page simplified — only Anthropic key needed
- **Key validation**: Save key → auto-tests with a real API call → green ✓ or red ✗
- **Config centralized**: `CLAUDE_MODEL`, `CLAUDE_MAX_TOKENS_*` all in one place (`config.py`)

### 3. Permission Fix
- **Manager role expanded**: Manager now sees all contacts/activities/leads (same as Admin)
- **Only SDR restricted**: SDR sees only their own data
- **Files changed**: `contacts.py`, `activities.py`, `dashboard.py`

### 4. Manager Accounts Created
- GT Marketing (marketing@graphictac.biz)
- GT Doug (graphictac.doug@gmail.com)
- GT Steve (graphictac.steve@gmail.com)
- All with role=manager, bcrypt-hashed passwords

### 5. Real Contact Data Imported (Local)
- **117 contacts** imported from `contacts_clean_for_crm.csv`
- City/State parsed from notes (Location: / Address: patterns)
- Industry tags split into arrays
- BOM encoding issue fixed (UTF-8-sig)

### 6. Backend Deployed to Railway
- **Commit**: `d6bc7e7` — hardcoded port 8080 for Railway
- **Commit**: `8e00615` — auto-convert `postgresql://` to `postgresql+asyncpg://` (Railway gives the wrong prefix for asyncpg)
- Railway auto-creates tables on first startup via `init_db.py`
- 4 user accounts created via API (1 Admin + 3 Managers)

---

## Day 2 (April 17) — Frontend Deploy + Data Migration

### 7. Frontend Deployment Struggles (6 attempts)
The frontend took multiple iterations to deploy correctly:

| Attempt | Problem | Fix |
|---------|---------|-----|
| 1 | Railway couldn't read frontend (was git submodule) | `0a39f87` — Converted from submodule to regular directory |
| 2 | "Could not find production build" | `b8ab9f6` — Added `buildCommand: "npm run build"` to railway.json |
| 3 | Build still not running | `5c6e064` — Changed to `"npm install && npm run build"` |
| 4 | Port issue | `fc995ad` — Used `$PORT` from Railway env |
| 5 | Railway still using Dockerfile instead of Nixpacks | `cadc1d7` — Deleted `Dockerfile` and `.dockerignore` from frontend |
| 6 | TypeScript build errors | `bd07678` — Fixed 3 type errors (SearchResult, SpeechRecognition, Suspense) |

### 8. CORS Fix
- **Commit**: `dd5e8da` — Changed `allow_origins` to `["*"]` for deployment phase
- Was blocking frontend from calling backend API

### 9. Cloud Data Import
- Imported all 117 contacts to cloud database via API calls
- 0 errors, 1 skipped (Nazdar — no first_name)
- Website, city/state, tags all preserved

---

## Files Changed (11 commits)

### Backend
| File | Change |
|------|--------|
| `app/main.py` | CORS opened to `["*"]`, removed `FRONTEND_URL` logic |
| `app/core/config.py` | Added `CLAUDE_MODEL`, token limits, removed OpenAI key |
| `app/core/database.py` | Auto-convert `postgresql://` → `postgresql+asyncpg://` |
| `app/services/ai.py` | Unified to Haiku 4.5, added `validate_key()`, `smart_search()`, OpenAI code commented out |
| `app/api/routes/ai.py` | New `/api/ai/search` (Claude direct), updated `/api/ai/status`, embedding endpoints disabled |
| `app/api/routes/contacts.py` | Added dedup check, `force_create` param, PUT endpoint, new fields |
| `app/api/routes/activities.py` | Manager sees all (was filtered) |
| `app/api/routes/dashboard.py` | Manager sees all (was filtered) |
| `app/api/routes/system_settings.py` | Removed OpenAI key endpoints, added key validation |
| `app/models/contact.py` | Added city, state, website, notes, industry_tags_arr, import fields |
| `app/schemas/contact.py` | Added all new fields to Create/Update/Response + validation |
| `railway.json` | Port 8080 hardcoded |
| `requirements.txt` | Added anthropic SDK |

### Frontend
| File | Change |
|------|--------|
| `railway.json` | Created — Nixpacks + npm install && npm run build |
| `package.json` | start: `next start -p 8080` |
| `Dockerfile` | **Deleted** (was causing Railway to use wrong builder) |
| `.dockerignore` | **Deleted** |
| `src/app/contacts/page.tsx` | Add Contact button, checkboxes, city/state/website display, Suspense boundary |
| `src/app/ai-search/page.tsx` | Rewritten — Claude Q&A style, removed Index button |
| `src/app/settings/page.tsx` | Removed OpenAI section, added key validation UI |
| `src/app/finder/page.tsx` | Fixed SearchResult type for TypeScript strict mode |
| `src/components/add-contact.tsx` | **New** — Full Add Contact modal |
| `src/components/quick-entry.tsx` | Fixed SpeechRecognition types |
| `src/components/email-compose.tsx` | Added AI Draft button |
| `src/lib/api.ts` | Removed OpenAI key methods, added checkEmail/updateFull |

### New Scripts
| File | Purpose |
|------|---------|
| `backend/seed_real_contacts.py` | Import 117 contacts from CSV |
| `backend/migrate_website_from_notes.py` | Extract website URLs from notes field |

### New Docs
| File | Purpose |
|------|---------|
| `docs/contacts_clean_for_crm.csv` | 118 real contact records |
| `docs/CONTACTS-ENHANCEMENT-SPEC.md` | Add/Import/Export feature spec |

---

## Current System Status

- **Backend**: Running on Railway ✅
- **Frontend**: Running on Railway ✅
- **Database**: Railway PostgreSQL with pgvector ✅
- **Users**: 4 accounts (1 Admin + 3 Manager) ✅
- **Contacts**: 117 imported ✅
- **AI**: Ready (needs Anthropic key in Settings) ⏳
- **Apollo**: Ready (needs Apollo key in Settings) ⏳
- **Email sending**: Records in CRM but no Gmail OAuth yet ⏳

---

*Generated by Claude Code · April 17, 2026*
