[pytest]
# Tests live under backend/tests, app code is rooted at backend/app.
# Adding `.` to pythonpath lets `from app.services...` imports resolve when
# pytest is invoked from the backend/ directory.
pythonpath = .
testpaths = tests
asyncio_mode = auto
